# Cloudflare routing.
#
# Blog routing is owned by the Worker (worker.js): it SSRs /blog/<slug> and
# 301s legacy /blog-<slug> → /blog/<slug>. Those rules are intentionally NOT
# here — the static-assets/_redirects layer runs BEFORE the Worker, so a rule
# here would shadow the Worker and prevent server-side rendering.
#
# (No active redirects at the moment. Add non-blog redirects below if needed.)
